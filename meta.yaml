{% set version = "0.5.0" %}

package:
  name: sketchy
  version: {{ version }}

source:
  path: . # [linux]

build:
  number: 0

requirements:
  build:
      - {{ compiler('cxx') }}
  host:
      - python>=3.7
      - rust>=1.39
  run:
      - python>=3.7
      - rust>=1.39
      - mash=2.2
      - wget
      - nextflow
      - tqdm
      - colorama
      - pandas
      - click
      - pytest
      - seaborn
      - scipy
      - python-dateutil
      - numpy
      - pysam
      - psutil
      - matplotlib-base
      - deprecation
      - braceexpand
      - networkx
      - pyfastx
      - watchdog
test:
  imports:
    - sketchy
  commands:
    - sketchy-utils --help
    - sketchy -h

about:
  home: 'https://github.com/esteinig/sketchy'
  license: MIT
  license_file: LICENSE
  summary: 'Genomic neighbor typing of bacterial pathogens using MinHash'

