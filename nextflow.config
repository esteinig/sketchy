params {

    executable = null

    sketch_mem = "32G"
    sketch_cpus = 8
    sketch_time = "6h"
}

process {
    withLabel: sketch {
        cpus = params.sketch_cpus
        memory = params.sketch_mem
        time = params.sketch_time
    }
}

profiles {
    local {
        process.executor = 'local'
    }
    slurm {
        process.executor = 'slurm'
    }
}